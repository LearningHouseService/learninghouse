# Sensitive values only. This file is never read from configuration.yaml,
# the environment, or written to logs. If it is missing, or missing
# jwt_secret, one is generated on first start and written here - copy this
# file only if you want to pin the value yourself (e.g. to share it across
# multiple processes).

# jwt_secret: replace-with-a-random-value
